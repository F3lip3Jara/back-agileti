name: Deploy Laravel PRD
on:
  push:
    branches:
      - main # Cambia esto si tu rama principal se llama 'master'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      # 1. Autenticación con Google Cloud
      - name: Google Auth
        uses: "google-github-actions/auth@v2"
        with:
          credentials_json: "${{ secrets.GCP_SA_KEY }}"

      # 2. Configurar Docker para Google Cloud
      - name: Setup Docker
        run: gcloud auth configure-docker --quiet

      # 3. Construir y subir la imagen al repositorio que ya tienes
      - name: Build and Push Image
        run: |
          docker build -t southamerica-east1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/cloud-run-source-deploy/laravel-app:latest .
          docker push southamerica-east1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/cloud-run-source-deploy/laravel-app:latest

      # 4. Desplegar en Cloud Run usando esa imagen
      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: back-agileti
          region: southamerica-east1
          image: southamerica-east1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/cloud-run-source-deploy/laravel-app:latest
          flags: "--update-secrets=/var/www/html/.env=Laravel-env:latest"
